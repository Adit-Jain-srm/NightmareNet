models:
  - name: distilbert-base-uncased
    type: seq_classification
    num_labels: 2

dataset:
  name: sst2
  split: validation
  text_column: sentence
  max_samples: 5

distortions:
  - type: dream
    strengths: [0.1, 0.3]

  - type: nightmare
    strengths: [0.1, 0.3]
